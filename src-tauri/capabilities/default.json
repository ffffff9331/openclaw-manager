{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口最小必要 IPC 权限：仅允许 OpenClaw 管理器当前实际使用的 shell / opener / dialog 能力。局域网实例访问不走 Tauri IPC，而由前端实例地址校验统一约束。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:allow-execute",
    "shell:allow-open",
    "dialog:default"
  ]
}
