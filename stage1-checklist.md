# openclaw manager v2 阶段一最终回归清单

日期：2026-04-02
状态：待人工执行盖章（代码与文档收口完成；正式完成待人工实测）
用途：在宣布阶段一正式完成前，快速确认当前实例主线与核心页面没有回退，并明确哪些项目已由自动回归覆盖、哪些仍需人工实测。

## 图例
- [x] 已由构建 / 代码现状 / 静态回归确认
- [ ] 仍需人工点按 / 启动应用确认

## 一、当前实例主线
- [x] 当前实例入口编排已从 `App.tsx` 下沉到 `useInstanceSelection.ts`
- [ ] 实例列表可正常显示
- [ ] 可切换当前实例
- [ ] 新增实例后可自动切换到新实例
- [ ] `Gateway / Models / Chat Channels / System / Tasks` 均围绕当前实例工作

## 二、Gateway
- [x] Gateway 控制动作已统一回到服务层主线，不再散落在页面拼装里
- [x] `GatewayPage` 已改成接收单块 `gatewayState`
- [ ] 可查看 Gateway 状态
- [ ] 可执行 `start / stop / restart`
- [ ] 刷新状态与日志不会报错

## 三、模型管理
- [x] `ModelsPage` 已拆出 `ModelFormModal / ModelProviderSection / ModelCard`
- [ ] 模型列表可正常加载
- [ ] 可新增模型
- [ ] 可编辑模型
- [ ] 可设置默认模型
- [ ] 可删除模型

## 四、渠道配置
- [x] `useChatChannels` 已收成统一保存动作与 `channelHandlers` 分发表
- [ ] 可切换渠道
- [ ] 可打开网关聊天入口
- [ ] 各渠道配置可加载
- [ ] 各渠道配置可保存
- [ ] 渠道启停动作可触发

## 五、系统与设置
- [x] `useSystemState` / `useSystemActions` 已分仓
- [x] `SettingsState` 已统一到共享类型层（`types/core.ts`）
- [x] `SettingsPage` 已拆出 `UninstallConfirmModal`
- [x] `SettingsPage` 已改成接收单块 `settingsState`
- [x] `allowLanAccess` 已接入新增实例地址校验主线
- [ ] 系统信息可读取
- [ ] Doctor 可执行
- [ ] App 日志可读取
- [ ] 深色模式切换正常
- [ ] 白名单 / 文件访问开关动作正常
- [ ] 局域网访问开关（`allowLanAccess`）动作正常
- [ ] 打开配置目录动作正常
- [ ] 卸载确认弹窗可正常打开/关闭

## 六、Tasks
- [x] 内建动作链已统一走运行时服务入口
- [ ] 自定义命令可新增
- [ ] 自定义命令可删除
- [ ] 命令可执行
- [ ] 内建动作链仍走统一运行时服务入口（人工点击复核）

## 七、构建回归
- [x] `npm run build` 通过

## 八、补充说明
- `mdnsEnabled` 当前仅为“预留偏好 / 规划中”，未接入自动发现流程；人工回归时不应把它当作当前版本真实功能点。

## 九、结论模板
### 自动结论
- 结构收口：通过 / 失败
- 构建：通过 / 失败
- 文档：通过 / 失败

### 人工结论
- 当前实例主线：通过 / 失败 / 未测
- Gateway：通过 / 失败 / 未测
- 模型管理：通过 / 失败 / 未测
- 渠道配置：通过 / 失败 / 未测
- 系统与设置：通过 / 失败 / 未测
- Tasks：通过 / 失败 / 未测

### 最终判断
- 是否允许宣布阶段一“代码与文档收口完成”：是 / 否
- 是否允许宣布阶段一“含人工实测的正式完成”：是 / 否
